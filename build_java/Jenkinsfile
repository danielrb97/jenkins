pipeline {
    agent any

        stages {
 
            stage ('test') {
                steps {
                  dir("build_java"){
                        bat "mvn clean compile test --fail-never"
                  }           
                   
                }
            }
        }
}
